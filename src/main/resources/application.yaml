spring:

  application:
    name: UsersAndSubscriptions

  datasource:
    url: jdbc:postgresql://localhost:5432/userssubscriptions
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql:
